/** Preserve supported ESLint peer floors and add newly adopted major ranges. */
export declare const resolvePeerEslintRange: (
    existingPeerRange: unknown,
    devDependencyEslintRange: string
) => string;
